```
export interface UploadPreviewSectionProps {
	sectionName?: string;
	sectionId?: string;
	className?: string;
	children?: React.ReactElement;
	actions?: BaseField[];
	showDivider?: Boolean;
	isUploaded?: Boolean;
}
```


- [ ] Use aadharCardVariant (for Aadhar card details)
- [x] Create a separate Image Preview or use Image tag for rendering the image