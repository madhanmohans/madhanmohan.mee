## Selenium: Web Testing Hero → End to end testing
Javascript executor will modify the DOM element for the Selenium to test
ChromeDriver
```CSS
js.executeScript()
```
Element was not interactable. Forcing interaction through JavaScript
can combine axios and promise to make a transaction
timeout: 1
transformRequest and transformResponse
- we can modify the request and response
interceptors → global handling of response and requests
for custom headers
Request Interceptors → Trasforms Requests → Server → Transform Response → Response Interceptors.
xsrf